const baseUrl = "https://my-item.obs.cn-south-1.myhuaweicloud.com"

const login = "/login"
const avatar = "/avatar"
const articles = "/articles"

const loginObs = baseUrl + login
const avatarObs = baseUrl + avatar
const articlesObs = baseUrl + articles

export { loginObs, avatarObs, articlesObs }